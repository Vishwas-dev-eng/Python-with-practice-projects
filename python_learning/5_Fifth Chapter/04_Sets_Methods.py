s = set() # This set is used for storing non-repetitive items 
a = { 3,33,3,3334 } # This set is initialized with some values, duplicates will be removed

a.add(34) # add method 
a.pop(  )# Removes and returns an arbitrary element from  the set 
a.remove(33) # removes the specific element from the set 
a.clear() # removes all the elements from the set

print(a)