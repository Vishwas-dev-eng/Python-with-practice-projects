name = 'Vishwas'
nameshort = name[0:3] # Start from the index 0 to all the way to index 3 (including  3 )
print(nameshort) 
nameshort = name[1:3] # Start from the index 1 to all the way to index 3 (excluding 1 )
print(nameshort)